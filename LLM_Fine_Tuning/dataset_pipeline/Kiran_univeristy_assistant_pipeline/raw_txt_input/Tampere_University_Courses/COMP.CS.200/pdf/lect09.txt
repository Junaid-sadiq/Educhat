Lecture 9 o Javascript in Document Object Model (DOM) manipulations: • Selecting elements • individual elements • collections of elements - live vs. static • Traversing elements • Adding/removing elements • Selecting and updating attributes 1 o Events ○Event object ○Event handler ○Passing parameters ○Event propagation How to access DOM tree elements? o Both id and class attributes functions as potential handles . . . <li id=″idx″ class=″hot″> content to the li element is something </li> . . . x: const x index Access individual elements o By their unique id value const x = document.getElementById("idx"); o Using CSS selectors const x = document.querySelector("li.hot"); x.className = "cool"; returns a reference to the first <li> element with the class 'hot' index Accessing several elements with querySelectorAll() o The following provides a NodeList of all the elements that satisfy the css selector const x = document.querySelectorAll("li.hot"); o NodeList is similar to an array, e.g., has a 'length' property o NodeList is accessed by indexing (similar to an array) x[0].className = "newValue" o Can use a 'for' statement to loop through each item in the collection 4 index getElementsByClassName getElementsByTagName o A reference to a collection of all the elements that belong to a particular class const x = document.getElementsByClassName("hot"); o A reference to a collection of all the <li> elements const x = document.getElementsByTagName("li"); 5 index Collections o A collection of references to the document objects that satisfy a particular condition (e.g., all with the same class name) const x = document.getElementsByClassName("hot"); //x.length==3 x[1].hidden = true; // access the second item . . . <li id= ″idx ″ class=″hot″> content to the li-element is something </li> . . . x: . . . <li id= ″ idx ″ class=″hot″> content to the li-element is something </li> . . . . . . <li id= ″idx″ class=″hot″> content to the li-element is something </li> . . . const x index Live and static collections o HTMLCollections returned by getElementsByXXX() are 'live'.. they are updated when the document is changed o NodeLists returned by querySelectorAll() are 'static'.. they reflect the state of the document when the query was made.. and aren't updated afterwards (so what? is this a problem?) index Traversing between elements o Each element on a page is represented as a node in DOM tree • nodes have methods and properties -e.g., properties for traversing: nextSibling, previousSibling, parentNode, firstChild, lastChild 8 index previousSibling, nextSibling o Properties of a node: previousSibling, nextSibling const listItem = document.getElementById("idx"); const x = listItem.nextSibling; • these are properties, not methods, thus, no brackets • if there is no 'nextSibling' then x is null • order of siblings is determined by their order in the DOM tree 9 index nextSibling const listItem = document.getElementById("idx"); const x = listItem.nextSibling; 10 listItem: X: li #text #text li #text li the same applies to previousSibling index Problem: white space between elements o Most browsers (except IE) treat whitespace in the html document as text nodes const x = listItem.nextSibling; const y = listItem.nextElementSibling; 11 listItem X li #text #text li #text li #text y index parentNode const listItem = document.getElementById("some-id"); const x = listItem.parentNode; 12 listItem with id=″some-id″ x h2 #text ul li li #text #text index firstElementChild,lastElementChild const listItem = document.getElementById("some-id"); const x = listItem.firstElementChild; 13 listItem X h2 #text ul li li #text #text index firstElementChild, lastElementChild const myList = document.getElementsByTagName("ul"); const x = myList[0].firstElementChild; 14 X h2 #text ul li li h1 #text script li li myList[0] index Text nodes: nodeValue o nodeValue accesses text from a node const listItem = document.getElementById("idx"); const x = listItem.firstChild; const line = x.nodeValue; o missing out x altogether line = listItem.firstChild.nodeValue; listItem.firstChild.nodeValue="new text"; 15 listItem X h2 #text ul li li #text #text index Text content of a node: textContent o nodeValue accesses text from a text node const listItem = document.getElementById("idx"); const line = listItem.textContent; listItem.textContent ="new text"; 16 listItem h2 #text ul li li #text #text index Adding elements to the DOM tree o we can create new elements, say an <h2> element const newHeader = document.createElement('h2'); o we can create new text nodes const newTxt = document.createTextNode("Great!"); o then append the text node to the new h2 node newHeader.appendChild(newTxt); o then append the new h2 node to the document somewhere, say to a div const parentDiv = document.getElementById("page"); parentDiv.appendChild(newHeader); 17 index Adding text element o We can create new elements, say, <h2> element const newHeader = document.createElement('h2'); o We can set the text of previously created element newHeader.innerText = "Hello World!"; o then append the new h2 node to the document somewhere, to a div, for example const parentDiv = document.getElementById("page"); parentDiv.appendChild(newHeader); 18 index Adding elements using innerHTML o with createTextNode() method or with textContent property we cannot include html code in the content of a node, but with innerHTML we can o create a new h2 element const newHeader = document.createElement('h2'); o write the code inside newHeader.innerHTML = "<strong>Great</strong> " + "job"; o and then insert the node into DOM const parentDiv = document.getElementById("page"); parentDiv.appendChild(newHeader); 19 index ADDED Removing elements from the DOM tree o store the element to be removed in a variable const removeThis = document.getElementById('losethis'); o store the parent of that element in a variable const parentEl = removeThis.parentNode; o remove the element from its parent parentEl.removeChild(removeThis); o Or simply remove it with command: removeThis.remove() 20 index Accessing and changing attributes o We use the methods hasAttribute() and getAttribute() const firstItem=document.getElementById("an-id"); if(firstItem.hasAttribute("class")){ const attrVal=firstItem.getAttribute("class"); } // attrVal now has value "hot" o use setAttribute() to give the attribute a value firstItem.setAttribute("class","cool"); 21 <div id="an-id" class="hot"> content… </div> <div id="an-id" class="cool"> content… </div> index Accessing and removing attributes o We use the methods hasAttribute() and removeAttribute() const button = document.getElementById("btn"); if (button.hasAttribute("disabled")){ button.removeAttribute("disabled"); } o use setAttribute() to give the attribute a value button.setAttribute("disabled", ""); 22 index Handling classes with JavaScript o Toggling removes the class if it already exists and adds it if it does not exist const firstItem=document.getElementById("an-id"); firstItem.classList.toggle("cool"); o Remove a class firstItem.classList.remove("cool"); o Check if class exists firstItem.classList.contains("cool"); o Add a class firstItem.classList.add("cool"); 23 <div id="an-id"> content… </div> <div id="an-id" class="cool"> content… </div> index Events - enable event-driven nature of JS - triggered by a user (or browser), e.g., user clicks a button or targets other interactive content - HTML Element then emits an event - To handle the event 1. Write a function that handles the event 2. Attach function to the element, multiple ways 24 index EventE mitter Event queue Event loop Event handlers Call stack Event object target - the target of the event, e.g., the element that was pressed or hovered (the same as this inside an eventhandler) keyCode - in case of a keypress, which key was pressed type - which event happened (e.g., click, keypress,...) 25 JS 14 tapahtuman kohde this Event types o related to • browser: load,unload,error,resize,scroll • keyboard: keydown, keyup,keypress • mouse: click,dblclick,mousedown,mouseup,mousemove,mouseover,mouseout • focus events: focus,blur • connected to forms (interactive elements) : input,change,submit, reset,cut,copy,paste,select • DOM modifications: DOMSubtreeModified,DOMNodeInserted, DOMNodeRemoved,MNodeInsertedIntodocument,DOMNodeRemovedFromdocument o more details in MDN 26 Events index Event as a parameter to event handler o The first parameter of every eventhandler is an event object, which • carries information of the event, which triggered the eventhandler • information accessed via event object’s properties and methods • the information includes, for example - the target of the event (e.target, the same as this inside an eventhandler function ) - what was the event (e.type) - if it was a key event, which key was pressed (e.keyCode) - If it was a mouse event, where in the window the event happened (e.clientX, e.offset) 27 function eventHandler() { let e = arguments[0]; e.target.style.color = red; } How an event is attached to an element? (1) HTML event-attributes, onevent • <a onclick="hide()"> old way – use only to quick testing (2) DOM event handlers • in JavaScript, for example, elem.onclick = hide; • element can be attached only with one function for each event (3) DOM event listeners • event can trigger several functions • for example, elem.addEventListener(″click″, hide); 28 index Adding event listener sets a function to be called asynchronously o An event can be attached with several event handlers per element 29 el.addEventListener('event', functionName [, Boolean]); element object event, such as ‘click’, ‘blur’,... the name of the event handler that will be performed, when the element launches the event (note: no parenthesis) ’bubbling’ / ’capturing’; false by default, which implies bubbling el.addEventListener index Addition and removal of a listener function checkTheLength() { ... if (this.value.length < 8) { // error message } } // get the reference to an element var el = document.getElementById('id'); // attach function checkTheLength to a blur event el.addEventListener('blur', checkTheLength, false) o The removal of a handler: el.removeEventListener = ('event', functionName [, Boolean]); o What if we want to pass parameters for the event handler? 30 index Passing parameters to an event handler o No parentheses - no parameters • Work-arounds: - a wrapper function, which calls the event handler function wrapper() { checkTheLength(8) } - the wrapper can also be anonymous: function () { checkTheLength(8) } 31 index Different handler functions - different scopes function checkTheLength(minLength) { let val = (this != window) ? this.value.length : null; if (!val && arguments.length > 1) val = arguments[1].value.length; if (val && val < minLength) console.log("Hello"); } // a reference to an element var el = document.getElementById('id'); // to attach function checkTheLength() to a blur event el.addEventListener('blur', function () { checkTheLength(8) }, false); //Not OK el.addEventListener('blur', () => { checkTheLength(8) }, false); //Not OK el.addEventListener('blur', () => { checkTheLength(8, el) }, false); //OK o problem: anonymous functions refer to a global scope (i.e., window object) 32 index Event propagation o HTML elements form a hierarchy o When an element receives an event (e.g. mouseover) the same event goes also to the ancestors o this matters only if an event is attached not only to an element but to its ancestors, as well 33 <a> <li> <ul> <body> <html> DOCUMENT event bubbling <a> <li> <ul> <body> <html> DOCUMENT event capturing index WINDOW WINDOW