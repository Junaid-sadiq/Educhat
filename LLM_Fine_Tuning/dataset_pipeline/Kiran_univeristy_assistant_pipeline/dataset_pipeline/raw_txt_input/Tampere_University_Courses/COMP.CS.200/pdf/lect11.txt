o Functional programming (FP) ○pure functions ■with no side-effects ○function as the 1st class citizen ○lambdas, i.e., anonymous functions ■ often Arrow functions (but need not to be) ● where this refers to global scope (window) ● no arguments object Lecture 11 1 o Higher-order functions ○map() ○filter() ○reduce() How is JavaScript as lang? (Kickoff) The syntax seems somewhat easy. To me, at this stage, it appears to be somewhat of a functional progamming language, but I'm not sure to what extent. Basically at this point I think it will be an easy language to use, even though I have almost no previous experience with it. One person from my team said that Javascript feels like Python but forced. I also did the first exercise in Javascript and found it a bit difficult to understand because, for instance, functions can be variables. We also thought that is a bit weird that numbers and strings can be equal. Also, the concept of hoisting is a bit weird. JavaScript is a bit confusing at the moment. Functions can be coded in multiple ways. No strict type operations. Variable scope messy. We think it's overall a powerful language but it would be easy to have hidden bugs such as indexing errors on arrays. It seems harder to get error messages on JavaScript than most other languages because it's so flexible. I've had a bit experience in writting JS and I see it as hard to debug without Type Script and the node modules. However, it has many different features and truly, my imagination is the limit. 2 3 Van Roy, Peter. "Programming paradigms for dummies: What every programmer should know." New computational paradigms for computer music 104 (2009): 616-621. 4 No side effects with pure functions A pure function - acts like a function in math - returns always the same output for an input, i.e., is idempotent - thus the return value must not rely on indeterminate values, such as Math.random() or Date - causes no side-effects, i.e., changes to state: - changes to its parameters passed by reference - modifications out of its scope, e.g., to global variables - by AJAX, timers, DOM manipulations - logging to a console - had the impure actions to be executed for desired functionality, purity principles would still be good to be kept in mind as much as possible 5 function f(x){ stateChanged = true; return "y"; } x let stateChanged = false; //global variable const y1= g(x); //"y1" relies on global variable const y = f(x); //"y" changes the state const y2 = g(x); //"y2" returns then a different value y y1 not pure function g(x) {return stateChanged?"y2":"y1"; } x y2 not pure const h = arr => arr.map(x=>x+"*purity"); [x1,x2, ...] [y1,y2, ...] Provides a secure, testable, and cacheable building block! Function as the first class citizen means that function can be, e.g., o passed as an argument o set as a variable o returned by a function o set as a callback (cb) Met-befores during WebCAIS: - element.addEventListener(type, cb) - setTimeout(cb, timeout) - custom higher-order functions 6 const a = par => `${par} is the best`; const b = func => func("Pure"); const c = (func,par)=>func(par); b(a); c(a,"Pure"); Arrow function o new in ES6, intended to • shorten the syntax • remove ambiguity about the current this object Conventional function as an expression (no name) const sayHello = function (greeting,sname) { console.log(greeting + " " + sname); } Arrow function const sayHello = (greeting,sname) => console.log(`${greeting} ${sname}`); 7 index Arrow function syntax (param1, param2) => { statement1; statement2} o if one parameter only, no round brackets param1 => {statement1; statement2} o if only one statement, no curly brackets (param1, param2) => statement1; o if no parameters, round brackets are needed () => statement1; o this refers to the global scope o What does the following function do? item=>item 8 index this in arrow functions const sname = "Boris"; function sayHelloforAll(greet){ console.log(greet+ " " + this.sname); } sayHelloforAll("Hello"); // Hello Boris const s2 = { sname:"Martta", sayHello: sayHelloforAll } const s3 = { sname:"Håkan", sayHello: sayHelloforAll } s2.sayHello("Hei"); // Hei Martta s3.sayHello("Hejsan"); // Hejsan Håkan 9 const sname = "Boris"; let sayHelloforAll = greet => console.log(greet + " " + this.sname); const s2 = { sname:"Martta", sayHello: sayHelloforAll } const s3 = { sname:"Håkan", sayHello: sayHelloforAll } s2.sayHello("Hei"); // Hei Boris s3.sayHello("Hejsan"); // Hejsan Boris Conventional function Arrow function No arrow function constructor let Student = (sname,age,homeTown) => { this.sname = sname; this.age = age; this.homeTown = homeTown;}; const s2 = Student("Anu", 27, "Orivesi"); o s2 is undefined o However, sname, age and homeTown are defined as global variables 10 index Arguments of an enclosing scope Conventional function 11 index // implicit arguments binding function test(n) { const f = () => arguments[0] + n; // arguments[0] is n return f(); } test(3); // 6 Arrow function const arguments = [1, 2, 3]; const arr = () => arguments[0]; // ref. to a global variable arr(); // 1 No arguments in arrow function o In conventional functions • the number of parameters may vary, but the variation can be handled with arguments o In arrow functions • NO own arguments object • arguments may refer to arguments of the enclosing scope depending on the situation 12 index Array functions for FP o Array.map() o Array.filter() o Array.reduce() o used to replace loops (for, while) o often implemented by calling arrow functions o are so called 'higher-order functions': functions calling functions 13 index Array.map() shortly let new_array = arr.map(function callback( currentValue[, index[, array]]) { // return element for new_array }[, thisArg]); thisArg - optional, value for ‘this’ in callback o Callback function is called for each item in array o A new array is constructed as a return value • The array should be of the same length as the array to be mapped • To return an array with different length is map Anti-pattern o The math analog of map() is a function - that maps a domain to a range - 14 index students array to be mapped const students = [ { "name": "Teemu Teekkari", "studentId": "tuni12345", "email": "teemu.teekkari@tuni.fi" }, { "name": "Teppo Testaaja", "studentId": "tuni23456", "email": "teppo.testaaja@tuni.fi" }, { "name": "Jaakko Javascript", "studentId": "tuni34567", "email": "jaakko.javascript@tuni.fi" } ] 15 index Array.map() has certain benefits over for loops for loop 16 index let names; const l = students.length; for (let i = 0; i < l; i++) { names.push(students[i].name); } console.log(names); Array.map() const names = students.map(std => std.name); // name implicitly returned Array.filter() shortly let newArray = arr.filter(callback(element[, index, [array]])[, thisArg]) //thisArg - optional, value for ‘this’ in callback o Callback function is called for each array item o callback returns • true to keep the element • false to discard the element o Array.filter() returns a new filtered array of kept elements 17 index students array to be filtered const students = [ { "name": "Teemu Teekkari", "studentId": "tuni12345", "email": "teemu.teekkari@tuni.fi", "major": "web&cloud" }, { "name": "Teppo Testaaja", "studentId": "tuni23456", "email": "teppo.testaaja@tuni.fi", "major": "web&cloud" }, { "name": "Jaakko Javascript", "studentId": "tuni34567", "email": "jaakko.javascript@tuni.fi", "major": "mathematics" } ] 18 index Array.filter() has certain benefits over for loops for loop 19 index let webMajors=[]; const MAJOR = "web&cloud"; const l = students.length; for (let i = 0; i < l; i++) { if (students[i].major===MAJOR) webMajors.push(students[i].name); } console.log(webMajors); Array.filter() const MAJOR = "web&cloud"; const webMajors = students.filter(std => std.major === MAJOR); Array.reduce() shortly arr.reduce(callback( accumulator, currentValue[, index[, array]] )[, initialValue]) o Callback function is called for every element in the array • Accumulator accumulates return values o often used to fold an array of values into a single value, e.g., sum or avg o Initial value is mandatory for an array of objects • 99% of the time you handle an array of objects 20 index shopping cart to be reduced const shoppingCart = [ { "name": "toothbrush", "category": "hygiene", "price": 2.99 }, { "name": "offBrandCoke", "category": "foodAndDrinks", "price": 0.99 }, { "name": "offBrandHamburger", "category": "foodAndDrinks", "price": 1.49 }, { "name": "offBrandFries", "category": "foodAndDrinks", "price": 1.49 } ] 21 index Array.reduce() has certain benefits over for loops for loop 22 index let total = 0; for (let i = 0; i < shoppingCart.length; i++) { total = total + shoppingCart[i].price; } Array.reduce() const total = shoppingCart.reduce(( sum, item ) => sum + item.price, 0) shopping cart reduced by category 1/2 const categories = ["hygiene", "foodAndDrinks"]; const pricesByCategory = categories.map(category => shoppingCart.filter(item => item.category === category) .reduce((priceObject, item) => { priceObject[item.category] = (priceObject[item.category] || 0) + item.price; return priceObject; }, {})); console.log(pricesByCategory) // [ { hygiene: 2.99 }, { foodAndDrinks: 3.96 } ] 23 index shopping cart reduced by category 2/2 const pricesByCategory2 = shoppingCart.reduce((obj,item) => { obj[item["category"]]=(obj[item["category"]] || 0)+item["price"]; return obj;}, {}) 24 index Promoted FP features for robustness 25 o const • to pave the way for immutability • more robust coding • easier bug tracking o lambdas o higher-order functions, e.g., map(), filter() and reduce() • make code more expressive • why Array functions, e.g., map(), are better than for loop: - return a new array instead of modifying the old one => immutability - no reassignable counters, thereby no accidental global vars - no such errors as infinite loops or off-by-one either index