Lecture 5 o CSS rule syntax selector[, selector]* {[prop: value;]+} o DOM tree and elements as targets o Selectors • element types • class/id attributes • other attributes • pseudo-classes/elements o Combinators for selectors 1 Styles, solving priorities o Specificity, cascading, inheritance o !important (to be avoided) o A general guideline for writing CSS o Box model o CSS Units (absolute, relative) o Viewport units, pixels CSS, Cascading Style Sheets o A stylesheet language used to style the HTML content on a web page o Cascading refers to inheritance • properties are inherited within the DOM hierarchy • from a parent to a child • inheritance is property-specific, not all properties are inherited o CSS1, CSS2, CSS2.1, CSS3 standards o no CSS4 coming, instead, W3C specifies different independent modules 2 Property indexes index 3 Canvas CSS rule syntax o Syntax of a CSS rule selectorList { declaration } o The same rule targeted at several elements o Several rules targeted at the same element 4 selector[, selector]* { [property: value;]+ } h1, h2 { /* headers blue */ color: blue; } body { /* page with black text */ color: black; /* on yellow background */ background-color: yellow; } selector property value NOTATIONS * index Example rules o Universal selector * selects all elements 5 * { /* unifying the marginal and box sizing */ margin: 0; box-sizing: border-box; } h1 { /* the first level header gets a black shadow */ text-shadow: 4px 4px 2px black; } p { /* text in paragraphs in red, and the width */ color: red; /* of the paragraph and the margin around it */ width: 240px; /* are defined */ margin: 3rem; } index DOM tree structure o DOM (document object model) • document comprises of hierarchy of nodes: element and text nodes 6 <!DOCTYPE html> <html lang="en"> <head> <meta charset="utf-8" > <title>HTML page</title> </head> <body> <h1> Elements on a page form the DOM tree structure </h1> <p> This page contains two paragraphs (this is the first of them). </p> <p> This is <em>second</em> paragraph and here is an image from <a href="http://www.visittampere.fi">Tampere</a><br> <img src = "img/01-tampere.jpg" alt="Tampereen Hämeensilta"> </p> </body> </html> html body head text text text em text text text text img br a p p meta h1 title href src in CodePen index Parent and child nodes • p node has six children, br node has five siblings • text node on the left is the first child of p node • em node has one child (text node) • a node is the parent of rightmost text node 7 em p text text text img br o DOM • describes the structure of the HTML document as a hierarchical node tree • important to understand when we want to modify the properties of a certain element on the page a text index Selectors: Element, Class, Id 8 o Element (type) selector o Class selector o Id selector p { color: red; } .important { color: red; } p.important { background: red } <p> text </p> <p class="important"> text </p> #my-name { color: red; } <p id="my-name"> text </p> HTML-document CSS-rules index Attribute selectors 9 o Elements may be selected on the basis of their attributes and attribute values • text color in both paragraphs is blue, only if lang is Finnish then background will be yellow o Another example, basic use of attribute selector • all the links which would open to a separate tab, are written with yellow background p[lang] { color: blue; } p[lang="fi"] { background-color: yellow </p> <p lang="en">text</p> <p lang="fi">teksti</p> [attribute[="value"]?] a[target="_blank"] { background-color: yellow } index More attribute selectors 10 [attribute^="value"] all elements starting with "value" (prefixed with "value") [attribute$="value"] all elem. ending with "value" (suffixed with "value") [attribute*="value"] all elem. containing "value" [attribute|="value"] E[lang|="en"] E elements whose "lang" attribute has a hyphen-separated list of values beginning (from the left) with "en", e.g., <span lang="en-fi"> [attribute~="value"] all elem. with one or more values for the attribute and one of them is "value" [attr1][attr2] all the elem with both attributes *= $= [attr] [attr] |= ^= ~= ^= $= *= |= ~= Selector Selects index Pseudo-class selectors 11 o "pseudo" because such class does not actually exist o Pseudo-class selector starts with a colon, p:first-child o Elements are selected neither by name nor attribute, but other properties of an element, e.g., on the basis of the present status (active, checked) :selector :only-of-type :optional :out-of-range :read-only :read-write :required :root :target :valid :visited :invalid :lang :last-child :last-of-type :link :not() :nth-child :nth-last-child :nth-last-of-type :nth-of-type :only-child :active :checked :disabled :empty :enabled :first :first-child :first-of-type :focus :hover :in-range :active :first-child index Pseudo-element selectors 12 o Pseudo-element selector starts with a double colon, e.g. a::before o Pseudo-class selector selects elements that actually exist o pseudo-element selector may create "pseudo-elements" that are not defined in HTML ::selector ::first-letter ::after ::after ::before ::first-letter ::first-line index Combinators o Combinators make use of the DOM tree structure e1 > e2 child combinator selects all e2 elements that have e1 as their parent e1 e2 descendant combinator selects all e2 elements that have e1 as a parent or as an ancestor e1 + e2 adjacent sibling selects the element e2, if e1 is the immediate older sibling e1 ~ e2 general sibling combinator selects all e2 elements that have e1 as an older sibling 13 ul a { background-color: yellow; } ul > li { background-color: yellow; } p + a { background-color: yellow; } li ~ li { background: #ff0000; } index Chaining selectors o By chaining selectors together you require that all the selectors match the element o For example when an element belongs to several classes o What is the difference between these two ? o Or what would this mean? 14 #header.callout { color:red; } #header .callout { color:red; } <h1 class="first third">Double Class</h1> .first.third { color: red; } #chapter-1.snippet.code.erroneous { color: red; } index Attaching style rules to a page o a separate *.css -file or several files •attached using the <link> element 15 <!DOCTYPE html> <html lang="fi"> <head> <meta charset="UTF-8" /> <title>Sivun otsikko</title> <link rel="stylesheet" type="text/css" href="path/basic.css" /> <link rel="stylesheet" type="text/css" href="path/navigation.css" /> <link rel="stylesheet" type="text/css" href="path/special.css" /> ... Method 1 index Attaching style rules to a page o <style> element in the head of the HTML document •for example, the CSS rules to set the background color and left margin of the whole page 16 <!DOCTYPE html> <html lang="fi"> <head> <meta charset="UTF-8" /> <title>Sivun otsikko</title> <style> body { background-color: maroon; margin-left: 20px; } p { color:white; } </style> ... Method 2 index o Straight into an element on a page • using the style-attribute o Method 1 is the preferred way to do it • CSS-files can be shared between several pages • method 2 can be used for small projects • method 3 only for quick experimenting – never leave style attributes in the finished code Attaching style to an element 17 Method 3 <p style="color:sienna; margin-left:20px;"> This is a paragraph. </p> Only the rule, no need for a selector index Several rules for the same element o How to decide the layout of an element? The following three concepts should be understood. o Specificity • selectors have a certain specificity value • the rule with the most specific selector "wins" o Cascading (location) • when several rules have same specificity value, the "last" rule wins o Inheritance • some properties are inherited from their parent (not all) 18 index Specificity o Each selector has a specificity value as follows o The specificity of a combined selectors are computed as a sum of the specificity values involved, e.g. • p::after • [type=email] • p.important • aside.chart:first-child 19 Selector Specificity Type selectors, pseudo-element selectors (e.g. p and ::after) 1 Class selectors, pseudo-class selectors, attribute selectors 10 Id-selector 100 Using style attribute within the element (inline style) 1000 - type and a pseudo-element selectors → 1+1 = 2 - an attribute selector → 10 - a type and a class selectors → 1 +10 =11 - a type , a class and a pseudo-class selectors → 1+10+10 =21 index Cascading o The location where the rule is given, can be • in the browser’s default styles (user agent style) • in the browser’s user defined styles (e.g. for Firefox via userContent.css -file) • in the document itself - in a separate CSS-file - in the <style> element in the head-part of the document - in the element with a style attribute o A rule given on an outer level (upper in the list) cascades inwards if no rule on an inner level overrides it 20 index more influential Inheritance o For example, color is inherited, but border isn’t • when you want to write the text on a page with blue - you give a rule for the body - you don’t need to give it for every element • but - when you give a list element (<ul>-element) a border line - it’s children (<li>-elements) are not drawn in boxes • however, a property can be changed to be inherited • notice, that properties can have initial values (for color, it can be transparent) 21 body { color: blue; } li { border: inherit; } index !important o All the rules previously assigned to an element can be overridden by writing !important after a rule o To be used in special situations only, for example • visually impaired users may write a rule in the user style sheet (browser file userContent.css) o Should not be used in normal style sheets, even if it sometimes feels tempting 22 body { font-size: large; !important } index A general guideline when writing CSS o Aim to keep the selectors general •you can use specific selectors, but only when really needed o CSS gets easily scattered •the more high-specificity selectors you use, the more difficult maintaining your stylesheet becomes 23 index Box model o content o padding o border o margin 24 content 0200 index box-sizing: border-box; box-sizing: content-box; Spacing between elements: padding and margin o Spacing between elements is handled with padding and margin properties • padding:[auto|value]1-4 • margin: [auto|value]1-4 o Padding keeps the content away from the border • behaviour quite straightforward and predictable • can not be negative o Margin leaves space between the border and other elements • horizontally pretty predictable • vertically margins of adjacent elements collapse • can be also negative • vertical placement inside the parent: margin: auto (!) h1, h2 { padding: 25px 50px; } p { padding: 25px 50px 5px; margin-left: 2em; } index content Margin behaviour horizontally 26 content margin: 20px margin: 60px margin: 80px index Margin behaviour vertically 27 content margin: 20px margin: 60px distance ? content 60 px – margins collapse vertically index Border o Border width, style, and color • border-width: [thin|medium|thick|measure|inherit]1-4 • border-style: [none|hidden|dotted|dashed|solid|double| inherit|groove|ridge|inset|outset]1-4 • border-color: [transparent|color|inherit]1-4 28 p { border-width: 10px; border-style: dashed; border-color: #34fade; } This would be the result if the rules were applied to a p element containing this text. p { border: 10px dotted #34fade; } Border index The box-sizing property can be used to add padding and border in an element's width and height: box-sizing: border-box; Border properties 29 div { border-width: 5px 15px 25px 35px; border-style: inset; border-color: blue; width: 30em; } div { border-width: 5px 10px; border-style: solid dotted; border-color: green; } div { border: 20px groove; border-color: green red yellow; } index Rounded corners 30 .framed-element { border: 10px double orange; border-radius: 5px 50px 50px 50px; } .framed-element { border-radius: 10px; } index CSS units (of length measure) o Used to define lengths and sizes in CSS rules o The absolute units: in, cm, mm, pt, pc • not useful on screen, might get used on some other medium o Pixel (px) is a very commonly used one, e.g, • min-width: 200px; font-size: 16px; o In addition to absolute, many units are relative • relative with respect to parent element or to the viewport (= visible part of the page) o Relative units include: %, em, rem, ex, ch, vh, vw • note: no space between the number and the unit, e.g. 16px, 0.75em, 1.25rem, 80%, 90vh, 85vw 31 index Relative units % • relative to parent dimensions, handy when the parent’s dimensions are set • however often tricky in the vertical dimension if parent element doesn’t have an explicit height set, the child percentage is always 0 em • relative to its parent’s font-size (height of the font) • usually the default font-size (for the <body>- element) in browsers is 16px rem • relative to the font size of the root element ex • height of the letter "x" (small x) in the used font ch • the width of the number "0" in the used font - 32 index Viewport units, pixels vh / vw • relative, like %-unit • instead of percentage of the parent, it is percentage of the screen (aka viewport) • vh and vw do not suffer from the parent limitation that the % unit does • vh refers to one percent of viewport height, and vw to one percent of viewport width px • pixels are useful for both horizontal and vertical dimensions • 1px = 1/96 of 1 inch, not relative • corresponds to one device pixel • high resolution (retina) displays 1px may be several device pixels • a pixel wide line is always sharp 33 index 100 vw = 100% of viewport width 100 vh = 100% of viewport height Examples o Default font-size (for the <body> element) in most browsers is 16px • 1em = 16px • 0.875em = 14px • 0.75em = 12px o Font size is one of the inherited properties • children inherit the text size of their parent • no need to repeat, unless text size is changed o Which units to use? • %-units for flexible width (changes with the window width) • em or rem units for font-sizes • px for fixed widths 34 li { /* list items are written */ font-size: 0.875em; /* smaller than its */ } q { /* quoted text will be half */ font-size: 0.5em; /* as big as its parent */ } index